# APK Pilot 0.5.1

Need help connecting opens a compact helper window. Restart connection helper is the primary action on that step and reports a real failure if the bundled helper does not restart. The wizard uses less vertical space, and the APK Pilot icon sits to the right of the product name.

Download: `SingularityApkInstaller-win-x64-setup.exe`
